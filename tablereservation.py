reservation_name = "Rudolf"

name = input("What is your name?: ") #prompting user for Name

if name == reservation_name: #checking if name matches the name in reservation_name
    print ("Right this way!") #If it does print "Right this way"

else:
    print ("Sorry, we don't have a reservation under that name") #If it does not then print "Sorry, we dont' have a reservation under that name
